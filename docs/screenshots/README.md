# Screenshots referenced from the project README

Drop the following PNG/JPG files into this directory:

| Filename | What's in it |
|---|---|
| `dashboard-hero.png` | Top of the dashboard — Hello/greeting, three stat cards (Net cashflow / Income / Expenses) |
| `dashboard-ai-cards.png` | Anomalies card + Forecast line chart + Subscriptions card |
| `chat-streaming.png` | The chat page mid-stream, with the blinking cursor visible |
| `transactions.png` | Transactions list with the Income/Expense filter pill |
| `auth-login.png` | Login page (clean — no error banner) |

You can rename them or add more — just update the `<img>` paths in `README.md`.

Recommended size: 1600px wide, PNG, dark theme (matches the brand).
